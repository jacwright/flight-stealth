/**
 *
 */
package flight.styles.hierarchical
{
	
	/**
	 * Style management class for a hierarchical styling system. This system is more complex than a global style system
	 * but provides greater flexibility and allows objects to be styled according to the location they're in rather than
	 * just based off their class name. This provides buttons inside of scroll bars to be styled differently from
	 * buttons elsewhere, for example.
	 */
	public class Styles
	{
		
	}
}
